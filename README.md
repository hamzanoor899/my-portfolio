## Hamza Noor — Portfolio

Built with **Next.js (App Router)**, **Tailwind CSS**, **GSAP**, and **Lenis**.

### Run locally

```bash
npm install
npm run dev
```

### Resume download

The "Download CV" button links to `/Hamza_Resume.pdf`.

Add your resume here:

- `public/Hamza_Resume.pdf`

### Smooth scrolling + animations

- Smooth scrolling: `components/SmoothScrollProvider.tsx` (Lenis)
- Reveal animations: `components/Reveal.tsx` (GSAP + ScrollTrigger)
